# 构建产物（VPK 走 GitHub Releases 发布，不进版本库）
dist/
*.vpk

# dependencies / tooling
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
__pycache__/
*.pyc

# python 打包（YUNYIN-TagCheck.exe 的产物目录）
build_tmp/
tools/*.exe

# 本地草稿（不进构建，也不进版本库）
app/model/

# 外部工具临时件（切图 / 图标压缩包，随时可重新生成）
图标/
图标.7z

# 标签体检报告（scripts/tagcheck.py 生成）
tag-report.csv
music-to-fix.m3u8
